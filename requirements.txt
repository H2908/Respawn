# Pinned deps for RecoveryBench reproducibility.
# Core library (respawn/) has zero runtime dependencies.
# Install with: pip install -r requirements.txt
anthropic==0.30.0
openai==1.35.0
matplotlib==3.8.4
numpy==1.26.4
tqdm==4.66.4
jsonlines==4.0.0
pytest==8.2.2
pytest-cov==5.0.0
ruff==0.4.9
